# WebTool

Fetch a URL and return clean readable text (HTML stripped).

**Module**: `tinytot.tools_ext`

## Constructor

```python
WebTool(self, /, *args, **kwargs)
```

Initialize self.  See help(type(self)) for accurate signature.

## Methods

### `run(self, url: 'str', max_chars: 'str' = '4000') -> 'str'`

### `schema(self) -> 'Dict[str, Any]'`

Return an OpenAI-compatible function-tool schema dict.
