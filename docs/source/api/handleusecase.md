# `handleUseCase`

**Module**: `tinytot.generate`

## `handleUseCase`

```python
handleUseCase(use_case: 'str', prompt: 'str') -> 'Optional[str]'
```

Dispatch to the handler for the detected use case.
