# SearchTool

DuckDuckGo lite search — returns top result snippets, no API key needed.

**Module**: `tinytot.tools_ext`

## Constructor

```python
SearchTool(self, /, *args, **kwargs)
```

Initialize self.  See help(type(self)) for accurate signature.

## Methods

### `run(self, query: 'str', num_results: 'str' = '5') -> 'str'`

### `schema(self) -> 'Dict[str, Any]'`

Return an OpenAI-compatible function-tool schema dict.
