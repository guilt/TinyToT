# `agentResponse`

**Module**: `tinytot.agent`

## `agentResponse`

```python
agentResponse(prompt: 'str', session_id: 'str' = '') -> 'str'
```

Entry point called from ``generateReasoningResponse`` when agent mode fires.
