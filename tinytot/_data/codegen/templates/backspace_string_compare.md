# Backspace String Compare

<\!-- Backspace String Compare Compare two strings after processing backspace characters. Example source = "ab#c" target = "adc" result = -->

## python
```python
source = "ab#c"
target = "adc"
result = Solution().backspaceCompare(source, target)
print(result)  # Output: True
```
